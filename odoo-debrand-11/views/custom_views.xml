<odoo>

    <template id="inherit_login_layout" name="Login Layout" inherit_id="web.login_layout">
        <xpath position="replace" expr="//div[1]">
             <div t-attf-class="card border-0 mx-auto bg-100 {{login_card_classes}} o_database_list" style="max-width: 300px;">
                    <div class="card-body">
                        <div t-attf-class="text-center pb-3 border-bottom {{'mb-3' if form_small else 'mb-4'}}">
                            <img t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}" alt="Logo" style="max-height:120px; max-width: 100%; width:auto"/>
                        </div>
                        <t t-out="0"/>
                        <div class="text-center small mt-4 pt-3 border-top" t-if="not disable_footer">
                            <t t-if="not disable_database_manager">
                                <a class="border-end pe-2 me-1" href="/web/database/manager">Manage Databases</a>
                            </t>
                           <a t-att-href="request.env.user.company_id.brand_url" target="_blank">Powered by
                                <span t-esc="request.env.user.company_id.brand_name" />
                            </a>
                        </div>
                    </div>
                </div>
        </xpath>
    </template>

    <template id="web.layout" name="Web layout">&lt;!DOCTYPE html&gt;
        <html t-att="html_data or {}">
            <head>
                <meta charset="utf-8" />
                <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />

                <!--                <title t-esc="title or request.env.company.brand_name" />-->
                <title />
                <link type="image/x-icon" rel="shortcut icon" t-att-href="x_icon or '/web/static/img/favicon.ico'" />

                <script id="web.layout.odooscript" type="text/javascript">
                    var odoo = {
                    csrf_token: "<t t-esc="request.csrf_token(None)" />",
                    debug: "<t t-esc="debug" />",
                    };
                </script>

                <t t-out="head or ''" />
            </head>
            <body t-att-class="body_classname">
                <t t-out="0" />
            </body>
        </html>
    </template>

    <template id="mail.discuss_public_channel_template" name="Discuss Public Template">&lt;!DOCTYPE html&gt;
        <html class="h-100">
            <head>
                <meta charset="utf-8"/>
                <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
<!--                <title t-esc="title or 'Odoo'"/>-->
                <link type="image/x-icon" rel="shortcut icon" t-att-href="x_icon or '/web/static/img/favicon.ico'"/>
                <script>
                    window.odoo = {
                        __session_info__: <t t-out="json.dumps(session_info)"/>,
                        csrf_token: "<t t-out="request.csrf_token(None)"/>",
                        debug: "<t t-out="debug"/>",
                        discuss_data: <t t-out="json.dumps(data)"/>
                    };
                    odoo.loadTemplatesPromise =
                    fetch(`/web/webclient/qweb/${odoo.__session_info__.cache_hashes.assets_discuss_public}?bundle=mail.assets_discuss_public`).then(doc
                    => doc.text());
                    {
                        const { user_context,  cache_hashes } = odoo.__session_info__;
                        fetch(`/web/webclient/translations/${cache_hashes.translations}?lang=${user_context.lang}`);
                    }
                </script>
                <t t-call-assets="mail.assets_public"/>
                <t t-call-assets="mail.assets_discuss_public_test_tours" t-if="'tests' in debug or test_mode_enabled"/>
                <style>
                    body {
                        direction: <t t-out="env['res.lang']._lang_get(lang or env.user.lang).direction or 'ltr'" />;
                    }
                </style>
            </head>
            <body class="d-flex flex-column h-100">
                <t t-out="body or ''"/>
            </body>
        </html>
    </template>

</odoo>